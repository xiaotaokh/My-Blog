{
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build --outDir dist",
    "preview": "vitepress preview --port 8080"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}